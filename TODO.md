# TODO

Volo 当前待办，按优先级粗排。
当前阶段：Windows 功能已经跑通，接下来从“能用”切到“可验证、可分发”。

## P0

- 发布前 QA 清单与验收
  先补一套跨平台冒烟测试清单，重点覆盖录音、识别、AI 修正、跨应用回填、托盘、快捷键、权限、异常日志和打包产物。把“稳定通过 / 偶发失败 / 明确失败条件”沉淀出来，作为正式分发前的 release gate。

- 识别链路可观测性
  当前已经有 Debug 模式和实时日志，但还需要把日志分阶段结构化，例如区分“等待音频提交 / ASR 请求 / AI 修正 / 自动回填”，这样更容易定位卡死点和失败位置。

- 更稳的错误提示与恢复
  需要继续梳理识别失败、回填失败、配置缺失、快捷键注册失败等场景的用户提示，并补一套更明确的重试和恢复路径。

## P1

- Windows 正式分发能力
  目前 Windows 包仍然是未签名状态，需要补代码签名，并决定正式分发形态是否保留 `portable + zip`，还是增加更适合普通用户的安装器和开始菜单入口。

- 自动更新能力
  需要设计完整的升级链路，包括发布产物规范、版本元数据、更新检查、下载安装、失败回滚、不同平台的签名与校验策略。

- 历史记录存储升级
  现在历史记录存在渲染层 `localStorage`，后续可以评估迁移到主进程持久化存储，避免 renderer 数据膨胀，也为未来跨设备同步、搜索、导出做准备。

- 调试工具增强
  可以继续补一键复制日志、导出日志文件、按时间筛选、按阶段筛选、日志截断策略，让线上排障更高效。

## P2

- 发布产物矩阵整理
  需要明确长期策略：Windows 是否同时提供 `x64` / `arm64`，macOS 是否区分 Apple Silicon / Intel，GitHub Release 的产物命名和说明是否要标准化。

- 配置导入导出
  支持词典、识别配置、快捷键配置的备份与恢复。

## Next

- 第一优先级：先做发布前 QA 清单与验收。
  这一阶段的目标不是继续堆功能，而是系统验证 Windows 和 macOS 的关键链路，整理出一份可重复执行的冒烟 checklist，并记录已知问题和复现条件。

- 第二优先级：根据 QA 结果补可观测性和错误恢复。
  哪些环节最容易失败，就优先补阶段日志、用户提示和恢复动作，避免后面做签名和分发时还在盲排问题。

- 第三优先级：开始 Windows 正式分发收口。
  当 QA 基本稳定后，再推进代码签名、安装器形态和发布说明，避免过早投入在会被返工的分发链路上。

## Recent Progress

- Windows 启动、打包、基础识别和跨应用回填已经跑通。
- Windows 默认快捷键切到 `Ctrl + Shift + Space`，避开 `Fn` 在 Windows 上不可用的问题。
- 托盘交互、气泡定位、日志清理和部分识别失败提示已经补齐。

## Backlog

- 自动更新后台服务选型
  评估 Electron 内建生态、自托管静态更新源，还是单独维护版本清单服务。

- 历史记录能力增强
  支持搜索、筛选、批量复制、导出。
